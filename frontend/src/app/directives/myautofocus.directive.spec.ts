import { MyautofocusDirective } from './myautofocus.directive';

describe('MyautofocusDirective', () => {
  it('should create an instance', () => {
    const directive = new MyautofocusDirective();
    expect(directive).toBeTruthy();
  });
});
